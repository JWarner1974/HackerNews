After cloning the project, it can be run from Visual Studio.

Once the project is running it can be tested in the browser with the url https://localhost:7020/storyGetBestStories/n where n is the number of stories requested (if omitted all available stories will be returned).
